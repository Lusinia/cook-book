const HOST_URL = 'http://localhost:1234';
export const BASE_URL = `${HOST_URL}/recipes`;
export const LOGIN_URL = `${HOST_URL}/login`;
export const REG_URL = `${HOST_URL}/register`;
export const LOGOUT_URL = `${HOST_URL}/logout`;
export const USER_URL = `${HOST_URL}/user`;

export const SM_WIDTH = 767;
export const MD_WIDTH = 1024;

export const SLIDING_TIME = 5000;
export const AUTH_MODES = { LOGIN: 'Login', REGISTER: 'Register', LOGOUT: 'Logout' };
